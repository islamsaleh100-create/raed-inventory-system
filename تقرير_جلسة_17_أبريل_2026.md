# تقرير جلسة التطوير — 17 أبريل 2026

**الهدف:** إنهاء المشاكل المتبقّية + تنفيذ Phase 7 من خطة التطوير بدون رجوع للمستخدم.

---

## ١. المشكلة المتبقية من الجلسة السابقة — ✅ مُنجزة

### endpoints `close` + `timeline` في router الطلبيات

الـ backend يحتوي على service functions (`close_order` و`get_order_timeline`)
لكن لم تكن مربوطة في الـ router. تم:

- `raed_inventory/backend/app/routers/orders.py` — أُضيف:
  - `POST /orders/{order_id}/close` → ينادي `orders_service.close_order`
    مع دعم `X-Idempotency-Key`. الصلاحية: warehouse_manager /
    operations_manager / admin / super_admin.
  - `GET /orders/{order_id}/timeline` → ينادي `orders_service.get_order_timeline`.
    الصلاحية: أي مستخدم مسجّل (مع فحص النطاق داخل الـ service).
- `raed_inventory/frontend/src/services/api.js` — أُضيف:
  - `ordersApi.close(id, client_request_id)`.
  - `ordersApi.timeline(id)`.
  - `ordersApi.cancel(id, reason)` (كانت ناقصة أيضًا).

---

## ٢. Phase 7A — CI/CD ✅

**الملفات الجديدة:**

- `.github/workflows/ci.yml` — 3 jobs:
  - **backend** (matrix: Python 3.11 + 3.12): install → alembic upgrade → pytest.
  - **frontend** (Node 20): npm ci → vite build → upload artifact.
  - **docker-build** (push only): smoke build للـ Dockerfiles مع GHA cache.
- `.github/workflows/security.yml` — 3 jobs أسبوعية:
  - `pip-audit` على backend requirements.
  - `npm audit` على frontend.
  - `gitleaks` لمسح الأسرار (secrets scanning).

---

## ٣. Phase 7B — Rate limiting على تسجيل الدخول ✅

- `raed_inventory/backend/app/core/limiter.py` (جديد) — يحمل `Limiter` مشترك
  + decorator آمن `@rate_limit(...)` يكون no-op إذا `RATE_LIMIT_ENABLED=false`.
- `raed_inventory/backend/app/main.py` — يستخدم الـ limiter المشترك بدلاً من
  إنشائه محليًا (حتى يستفيد منه الـ routers).
- `raed_inventory/backend/app/routers/auth.py` — الـ endpoint
  `/auth/login` أصبح محميًا بـ `@rate_limit(settings.RATE_LIMIT_AUTH)`
  = **20 محاولة/دقيقة/IP** افتراضيًا. يمنع brute-force على كلمات المرور.

---

## ٤. Phase 7C — Structured logging + Sentry scaffold ✅

### Backend

- `app/core/request_context.py` (جديد):
  - `RequestIdMiddleware` يولّد UUID لكل طلب (أو يحترم `X-Request-ID` إن أتى).
  - يُضيفه في response header وفي ContextVar متاح للـ log filter.
  - `RequestIdLogFilter` يحقن `request_id` في كل سطر لوج — الآن JSON logs
    تحتوي على `request_id` تلقائيًا.
- `app/core/sentry_init.py` (جديد):
  - `init_sentry()` يبدأ Sentry فقط إذا `SENTRY_DSN` موجود و`sentry-sdk` مثبّت.
  - `traces_sample_rate = 0.1` في production/staging، 0 في local.
  - `before_send` يُسقط Authorization/Cookie/X-Idempotency-Key من الأحداث.
- `app/main.py` — يستورد `init_sentry()` بعد `setup_logging()` مباشرة،
  ويضيف `RequestIdMiddleware` (outermost) بعد `TenantMiddleware`.
- `app/core/logging_config.py` — يُلحق `RequestIdLogFilter` بالـ handler.

### Frontend

- `src/utils/sentry.js` (جديد) — `initSentry()` اختياري، يستخدم
  `VITE_SENTRY_DSN`. `@sentry/react` ليست dependency إلزامية — إذا لم تُثبَّت
  يتمّ الـ skip بهدوء.
- `src/main.jsx` — ينادي `initSentry()` قبل render.

---

## ٥. Phase 7D — Backup + pre-commit ✅

- `backend/scripts/backup_db.sh` (جديد):
  - `pg_dump --format=plain | gzip -9` → ملف مُؤرَّخ.
  - Integrity check (gzip -t + non-empty).
  - S3 upload اختياري (`STANDARD_IA` storage class).
  - Rotation تلقائي (احذف > BACKUP_RETENTION_DAYS).
  - توثيق cron scheduling في رأس الملف (03:00 Asia/Riyadh).
- `backend/scripts/RESTORE_PROCEDURE.md` (جديد) — إجراء استعادة خطوة-بخطوة
  مع RTO/RPO واختبار استعادة شهري.
- `backend/scripts/check_alembic_chain.py` (جديد) — pre-commit hook يتحقّق من
  سلامة سلسلة migrations (لا branches، head واحد، سلسلة كاملة).
  **تم اختباره محليًا: `[OK] alembic chain verified (6 revisions, 1 head)`**.
- `.pre-commit-config.yaml` (جديد) — hooks:
  - sanity (trailing-whitespace, end-of-file, check-yaml/json/toml).
  - `detect-private-key`, `check-added-large-files` (1MB).
  - `ruff` + `ruff-format` على backend.
  - `gitleaks` (secrets scan).
  - `alembic-down-revision` (local).

---

## ٦. Phase 7E — الأدلّة بالعربية ✅

- `docs/ADMIN_GUIDE.md` — دليل المسؤول (super_admin/admin/operations_manager):
  الأدوار، التشغيل الأوّل، إدارة المستخدمين، Master Data، دورة الطلبيات،
  النسخ الاحتياطي، المراقبة، استكشاف الأعطال.
- `docs/BRANCH_USER_GUIDE.md` — دليل مستخدم الفرع (branch_manager/branch_user):
  تسجيل الدخول، الجرد اليومي، الطلبيات، التحويلات، زيارة الجودة، أخطاء شائعة،
  نصائح تشغيل.

---

## ٧. تحليل الـ 15 اختبار المتبقّي — ✅ موثَّق

- `backend/TESTS_REMAINING_ANALYSIS.md` — تصنيف في 5 فئات مع خطوات إصلاح
  محدّدة لكل فئة وتقدير الوقت (2-3 ساعات كلّي) — لإعطاء Cursor كـ reference
  عند المتابعة.

---

## ٨. ما لم يُنفَّذ ولماذا

- **تشغيل pytest للتحقّق من عدم وجود regressions**: البيئة لا يوجد بها
  اتصال إنترنت (proxy 403 عند تثبيت `fastapi`)، لذا لا يمكن تشغيل الاختبارات
  داخل الـ sandbox. **خطوة التحقّق المقترحة:** عند عودة المستخدم للاب،
  يشغّل:
  ```bash
  cd raed_inventory/backend
  pytest --tb=short -q 2>&1 | tee /tmp/post_phase7.log
  grep -E "^FAILED|^ERROR" /tmp/post_phase7.log | wc -l
  ```
  يجب أن يبقى العدد ≤ 15 (نفس ما كان بعد إصلاحات Cursor).

- **تطبيق fixes الفئات الـ 5 من `TESTS_REMAINING_ANALYSIS.md`**: تتطلّب
  تشغيل pytest للتحقّق التكراري من كل فشل — غير ممكن في الـ sandbox.
  **مستحسن:** أن يسلّم المستخدم الملف لـ Cursor مع الأمر
  "طبّق الفئات 1-5 واحدة تلو الأخرى".

- **git commit**: `git` غير متاح في الـ sandbox. جميع التعديلات محفوظة في
  `/mnt/raed_inventory_system/...` وستظهر للمستخدم عند فتح IDE.

---

## ٩. الخطوات التالية الموصى بها

### فوري (عند عودة المستخدم)

1. شغّل `pytest` وقارن العدد بالـ 15 قبل التغييرات.
2. شغّل الـ backend محليًا وتحقّق من `/api/docs` (يجب أن تظهر
   `POST /orders/{id}/close` و`GET /orders/{id}/timeline`).
3. `git status` → سترى ~15 ملف جديد/معدّل.

### خلال الأسبوع

4. ادفع إلى GitHub على branch `feature/phase-7-hardening`.
5. فعّل GitHub Actions (سيجري CI تلقائيًا على الـ PR).
6. ثبّت `pre-commit` محليًا: `pip install pre-commit && pre-commit install`.
7. سلّم `TESTS_REMAINING_ANALYSIS.md` لـ Cursor لإنهاء الـ 15 اختبار.

### خلال الشهر

8. نفّذ Phase 8 من `خطة_تطوير_رائد.md` (PWA offline + barcode + smart prefill).

---

## ١٠. ملفات المرجع الحالية (في الـ workspace)

- `خطة_تطوير_رائد.md` — خطة التطوير طويلة المدى (Phases 7-10).
- `مراجعة_شاملة_النظام.md` — مراجعة النظام من الجلسة الأسبق.
- `تقرير_جلسة_17_أبريل_2026.md` — هذا التقرير (ما أُنجز اليوم).
- `raed_inventory/backend/TESTS_FAILURE_TRIAGE.md` — triage أصلي من Codex.
- `raed_inventory/backend/TESTS_REMAINING_ANALYSIS.md` — تحليل المتبقّي.
- `raed_inventory/backend/STAGING_MIGRATION_PREVIEW.sql` — DDL للـ migration 0004.
- `raed_inventory/backend/PRE_MIGRATION_DATA_CHECK.md` — تحقّق قبل الـ migration.
- `raed_inventory/backend/scripts/RESTORE_PROCEDURE.md` — إجراء استعادة DB.
- `raed_inventory/docs/ADMIN_GUIDE.md` — دليل المسؤول.
- `raed_inventory/docs/BRANCH_USER_GUIDE.md` — دليل الفرع.

*مُنجز بالكامل — 17 أبريل 2026.*
