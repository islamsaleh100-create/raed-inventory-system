# تقرير المراجعة الشاملة لنظام Raed Inventory

**تاريخ المراجعة:** 2026-04-17
**النطاق:** Backend (FastAPI) + Frontend (React/Vite) + Database (SQLAlchemy + Alembic)
**المراجع:** مراجعة آلية معمّقة لكامل الكود
**إجمالي الملفات المفحوصة:** ~100 ملف (routers, services, models, pages, components, migrations, tests, logs)

---

## 📋 الملخص التنفيذي

البرنامج في حالة **متقدمة وظيفياً** ويغطي 32 فرعاً ومستودعَين و8 أدوار مختلفة، مع ميزات قوية (ledger، idempotency، multi-tenant foundation). لكن هناك **مشاكل جوهرية** يجب حلها **قبل أي نشر فعلي (production)** لأنها تسبب أحد ثلاثة أمور:

1. **كسر كامل للأمان** (أي مستخدم يرى بيانات الكل).
2. **فساد أرصدة المخزون** (أرقام خاطئة، بيانات ضائعة).
3. **500 errors** تمنع المستخدم من إنجاز عمله.

### نظرة سريعة على الأعداد

| الفئة | عدد المشاكل | الأشد خطورة |
|-------|-------------|-------------|
| 🔴 حرجة (Critical) | 12 | IDOR في ledger/variance + Race conditions في المخزون |
| 🟠 عالية (High) | 15 | JWT default secret + weak passwords + partial receipts |
| 🟡 متوسطة (Medium) | 18 | Missing indexes + timezone + demo creds exposed |
| 🟢 منخفضة (Low) | 10 | Code bloat + dead components + Hijri dates |
| **المجموع** | **55** | |

---

## 🔴 أولاً: المشاكل الحرجة (Critical — يجب إصلاحها فوراً)

### 1. خطأ 500 فعلي الآن عند فتح تفاصيل طلبية (`GET /api/orders/{id}`)

**الدليل من اللوج الحي:**

```
File "C:\raed_inventory_system\...\routers\orders.py", line 147, in get_order
    joinedload(ReplenishmentOrder.lines).joinedload(ReplenishmentOrderLine.item).joinedload(
sqlalchemy.exc.ArgumentError: expected ORM mapped attribute for loader strategy argument
INFO:     127.0.0.1:25051 - "GET /api/orders/4 HTTP/1.1" 500 Internal Server Error
```

الكود الحالي في الـ workspace تم تحديثه ليستخدم `selectinload` بدل `joinedload` — لكن **الخطأ متكرر عشرات المرات في اللوج**، بمعنى أن نسخة الكود على جهازك (`C:\raed_inventory_system\...`) **لم يتم إعادة تشغيلها بعد التعديل**، أو يوجد تسلسل `joinedload` ناقص في مكان آخر.

**الإجراء المطلوب:**
- إعادة تشغيل `uvicorn` (طالب backend كامل) بعد كل تعديل في الـ router.
- التأكد أن `ReplenishmentOrderLine.item` هو فعلاً `relationship()` وليس `Column` (يجب فحصه).

---

### 2. ثغرة IDOR في تقارير الـ Ledger — أي مستخدم يرى بيانات أي فرع

**الملف:** `app/routers/ledger.py:21-46` + `app/services/ledger_service.py:35-100`

الـ endpoint يستقبل `branch_id` من المستخدم ويمرّره للـ service **دون التحقق من صلاحية المستخدم على هذا الفرع**. التعليق في الكود يدّعي أن "enforced by access guard in service" لكن الـ service **لا يستقبل `current_user` أصلاً**.

**مثال هجوم:**
- المستخدم `branch.user2` مرتبط بفرع رقم 10 (Arkan).
- يطلب `GET /api/v1/ledger/branches/8` ← يحصل على كل حركات فرع 8 (Onda Riyadh).
- نفس المشكلة في `get_variance_report()`.

**الإصلاح:**
```python
# في app/routers/ledger.py
def get_branch_ledger(branch_id: int, current_user: User = Depends(get_current_active_user), ...):
    if not can_access_branch(current_user, branch_id):
        raise HTTPException(403, "ليس لديك صلاحية على هذا الفرع")
    return ledger_service.get_branch_ledger(db, branch_id, current_user)
```

---

### 3. تحويل المخزون بين المستودعات لا يتحقق من صلاحية المصدر

**الملف:** `app/services/stock_adjustment_service.py:272-338` — `transfer_warehouse_to_branch()`

الدالة تتحقق من الدور (warehouse_manager/user) لكن **لا تتحقق أن المستخدم مرتبط بالمستودع المصدر**. نتيجة: `wh.user1` (مرتبط بمستودع الرياض) يستطيع صرف من مستودع الدمام.

**الإصلاح:**
```python
if not can_access_warehouse(current_user, warehouse_id):
    raise HTTPException(403, "ليس لديك صلاحية على هذا المستودع")
```

---

### 4. JWT Secret Key مُحدَّد في الكود (Fallback خطر)

**الملف:** `app/config.py:17`

```python
SECRET_KEY: str = "raed-super-secret-key-change-in-production-2025"
```

لو تم نشر البرنامج على خادم بدون تعيين متغير بيئة `SECRET_KEY`، **أي شخص يعرف الكود يستطيع توليد JWT صحيح لأي مستخدم** (بما فيهم admin).

**الإصلاح:**
```python
SECRET_KEY: str = os.environ["SECRET_KEY"]  # يرفع خطأ إذا لم يُحدَّد
# أو:
if SECRET_KEY == "raed-super-secret-key-change-in-production-2025" and not DEBUG:
    raise RuntimeError("يجب تعيين SECRET_KEY في production")
```

---

### 5. Race Conditions في حركات المخزون — بدون Row Locking

**الملفات:**
- `app/services/orders_service.py:409-630` (dispatch/receive)
- `app/services/stock_adjustment_service.py:97-181`
- `app/services/inventory_service.py:370-438`

كل الكود يعمل `db.query(WarehouseStock).filter(...).first()` ثم يعدّل الكمية — **بدون `.with_for_update()`**.

**سيناريو فساد محتمل:**
- المستودع به 100 وحدة صنف X.
- مستخدمان يضغطان "صرف 60" في نفس اللحظة.
- الاثنان يقرآن `current_qty=100`، كلاهما يكتب `current_qty=40`.
- النتيجة: تم صرف 120 من مخزون 100 فعلياً، لكن السجل يقول 40.

**الإصلاح:**
```python
wh_stock = db.query(WarehouseStock).filter(...).with_for_update().first()
```

---

### 6. الموافقة على الجرد يمكن أن تحدث مرتين (Double Adjustment)

**الملف:** `app/services/inventory_service.py:175-219`

الدالة تستخدم `client_request_id` لضمان idempotency، لكن **إذا كان `None` فإنها تتابع كما لو كانت مرة جديدة**. النتيجة: نفس جلسة الجرد يمكن الموافقة عليها مرتين، وكل مرة تُعدِّل المخزون بنفس القيم → أرصدة مضاعفة خطأ.

**الإصلاح:**
- إلزام `client_request_id` كحقل مطلوب.
- أو إضافة فحص منفصل: `if inventory.status == "approved": raise 409`.

---

### 7. المستودع يوافق على كميات أكبر من المتوفر (No Stock Check)

**الملف:** `app/services/orders_service.py:231-298` — `warehouse_approve_order()`

الدالة تمرّر `branch_requested_qty` إلى `wh_approved_qty` دون التحقق أن المستودع يملك هذه الكمية. نتيجة:

- المستودع به 100 وحدة فقط من صنف X.
- الفرع يطلب 500. مدير المستودع يضغط "موافقة".
- النظام يعتمد 500 بدون شكوى. وقت الصرف: فشل صامت أو صرف جزئي.

**الإصلاح:**
```python
if line.wh_approved_qty > wh_stock.current_qty:
    raise HTTPException(400, f"الكمية المعتمدة تتجاوز المتاح ({wh_stock.current_qty})")
```

---

### 8. الصرف يُخفي العجز صامتاً (Silent Stock Loss)

**الملف:** `app/services/orders_service.py:468`

```python
wh_stock.current_qty = max(Decimal("0"), wh_stock.current_qty - dispatched_qty)
```

**الـ `max(0, ...)` يخفي الخطأ**: لو حاول المستخدم صرف 100 من مخزون 60، يصبح المخزون 0 وتختفي 40 قطعة من السجل **دون أي log أو alert**.

**الإصلاح:** رفع خطأ صريح:
```python
if wh_stock.current_qty < dispatched_qty:
    raise HTTPException(400, "الكمية المطلوبة غير متوفرة")
```

---

### 9. الطلبية تُغلق رغم نقص الاستلام (Closed With Missing Items)

**الملف:** `app/services/orders_service.py:556-616`

لو صُرف 100 واستُلم 70 (5 مفقودة، 25 مرفوضة):
- `line_status` يُضبط على "received".
- الطلبية تصبح `OrderStatus.closed`.
- **30 قطعة اختفت**: المستودع خصمها، الفرع لم يستلمها، ولا يوجد reversing transaction.

**الإصلاح:** إضافة reconciliation — أي فارق بين dispatched و received يُرجَع للمستودع كـ `adjustment_in` أو يُحوَّل لـ "investigation_required".

---

### 10. المخزون السالب مسموح على مستوى قاعدة البيانات

**الملف:** `app/models/__init__.py` — جداول `branch_stock` و `warehouse_stock`

لا يوجد `CHECK(current_qty >= 0)` في الـ migration. أي bug في الكود أو أي INSERT يدوي يمكن أن ينشئ رصيد سالب دون شكوى.

**الإصلاح:** إضافة migration جديد:
```python
op.create_check_constraint("ck_branch_stock_qty_nonneg",
    "branch_stock", "current_qty >= 0 AND reserved_qty >= 0 AND in_transit_qty >= 0")
```

---

### 11. StockTransaction بدون Foreign Keys على source/destination

**الملف:** `app/models/__init__.py` — جدول `stock_transactions`

```python
source_type = Column(String(50))      # "branch" أو "warehouse" — بدون CHECK
source_id = Column(Integer)           # بدون FK!
destination_type = Column(String(50))
destination_id = Column(Integer)      # بدون FK!
```

**الأثر:** يمكن حذف فرع وتبقى سجلات `stock_transactions` تشير إليه (orphans)، ولا يوجد ما يمنع `source_id=999999` غير موجود.

**الإصلاح:** إما FK منفصلان (`source_branch_id`, `source_warehouse_id`) أو CHECK constraint + التحقق في التطبيق.

---

### 12. Auto-Replenishment ليس له Scheduler حقيقي

**الملف:** `app/services/replenishment_service.py:135-242`

الدالة `generate_replenishment_order()` مصمّمة بشكل جيد لكن **تُستدعى فقط من `inventory_service.py:199`** عند اعتماد الجرد اليومي. لا يوجد cron أو background worker.

**النتيجة:**
- لو يوم الجرد لم يُعتمد (أجازة، خلل، نسيان) → لا تُنتج طلبيات أوتوماتيكية رغم انخفاض المخزون.
- الـ 204 طلبية "auto_replenishment" في الـ DB كلها نتجت من اعتماد جرد، ليس من فحص دوري للمخزون.

**الإصلاح:** إضافة APScheduler أو Celery beat:
```python
# في app/main.py أو startup
from apscheduler.schedulers.background import BackgroundScheduler
scheduler = BackgroundScheduler()
scheduler.add_job(check_all_branches_for_replenishment, 'cron', hour=6)  # كل يوم 6 صباحاً
scheduler.start()
```

---

## 🟠 ثانياً: المشاكل العالية (High Priority)

### 13. بيانات Demo Credentials مكشوفة في صفحة الـ Login

**الملف:** `frontend/src/pages/auth/LoginPage.jsx:110-127`

أزرار "Demo: admin / branch.mgr1 / wh.user1 / ...." + كلمات السر، مكتوبة في الـ JSX. أي شخص يفتح الصفحة يراها في production.

**الإصلاح:**
```jsx
{import.meta.env.DEV && <DemoCredentialButtons />}
```

---

### 14. الـ Tokens مُخزَّنة في localStorage (XSS Vulnerable)

**الملف:** `frontend/src/services/api.js:9` + `src/store/index.js:4-22`

```javascript
localStorage.setItem('access_token', token);  // أي XSS يسرقها
```

**الأثر:** إذا تم حقن أي سكريبت (عبر اسم صنف خبيث، comment في order، إلخ)، يُسرَق التوكن.

**الإصلاح:**
- الأفضل: نقل التوكن لـ `httpOnly` Cookie (يتطلب تعديل backend ليقبلها).
- البديل المؤقت: CSP headers صارمة + DOMPurify على أي input يُعرض.

---

### 15. Password Validation غير متسقة

**الملفات:**
- `app/routers/auth.py:86` → `/auth/change-password` يقبل 6 أحرف فقط.
- `app/routers/users.py:18-23` → إنشاء المستخدم يتطلب 8 أحرف + كبير + رقم.

مستخدم موجود يستطيع تغيير كلمة سره إلى "123456" بينما المستخدم الجديد لا يستطيع. **نفس الدالة `_validate_password()` يجب أن تُستدعى في الموضعين.**

---

### 16. DEBUG = True افتراضياً

**الملف:** `app/config.py:25`

```python
DEBUG: bool = True
```

في الـ production: يُسرِّب stack traces كاملة + تفاصيل داخلية عند أي خطأ. يجب أن يكون `False` افتراضياً و يُفعَّل بـ env variable.

---

### 17. رفع الملفات بدون حد حجم

**الملف:** `app/routers/import_data.py:77-108`

```python
content = file.file.read()  # بلا حد!
```

رفع ملف 10 جيجا يأكل الذاكرة → DoS. يجب فحص `file.size` قبل `.read()`.

---

### 18. التوقيت (Timezone) UTC بدلاً من AST

كل `datetime.utcnow()` في الكود + DB تخزّن UTC. في السعودية: فرق 3 ساعات.

**أمثلة المشاكل:**
- جرد تم إدخاله الساعة 1 صباحاً الخميس (توقيت السعودية) = 10 مساء الأربعاء UTC.
- تقارير "مبيعات اليوم" تبدأ من 3 صباحاً (منتصف ليل UTC).
- طلبيات تُقفل/تُفتح على تاريخ خاطئ.

**الإصلاح:**
```python
from zoneinfo import ZoneInfo
AST = ZoneInfo("Asia/Riyadh")
datetime.now(AST).date()  # للعمليات اليومية
```

---

### 19. الموافقة الجزئية تسمح بالـ Picking

**الملف:** `app/services/orders_service.py:386`

```python
if order.status not in [OrderStatus.approved, OrderStatus.partially_approved]:
```

الطلبية بحالة "موافقة جزئية" (بعض الأسطر مرفوضة) تسمح ببدء الـ picking. العاملون قد يلتقطون سطراً مرفوضاً لأن الحالة غير واضحة.

---

### 20. Inter-Branch Transfer غير مُنفَّذ أصلاً

المذكور في المهام: "نموذج OrderType.inter_branch جديد". البحث في الكود:

```bash
grep -r "inter_branch" app/  # لا يوجد
```

هذا ليس bug، بل **ميزة لم تُكتب بعد** رغم تعيينها في الوثائق.

---

### 21. 30 فرعاً من 32 بدون مستخدمين

حسب البيانات المذكورة:
- فقط فرع 8 (Riyadh Kitchen) و فرع 10 (Arkan) لهما مستخدمان.
- الـ 30 فرع الآخر: لا أحد يستطيع الدخول للنظام باسم مدير/مستخدم الفرع.

**نتيجة عملية:** برنامج يحتوي 32 فرعاً، لكن 94% من الفروع لا تستطيع إدخال جرد أو طلب replenishment.

**الإصلاح المطلوب:** script يولّد users لكل فرع (مثلاً `branch.mgr.ryd_olaya`, `branch.user.ryd_olaya`).

---

### 22. No Error Boundaries في الـ Frontend

أي crash في أي component يكسر **الصفحة كاملة أو التطبيق كله**. يجب إضافة:
```jsx
<ErrorBoundary fallback={<ErrorFallback />}>
  <Routes>...</Routes>
</ErrorBoundary>
```

---

### 23. عرض أسماء الأصناف غير متسق

الـ fallback pattern يختلف بين الصفحات:
- ✅ `ManualOrderPage`: `item.item_name_ar || item.item_name_en || item.item_code`
- ❌ `InventoryListPage`: مباشرة `item.item_name_ar` (بدون fallback)
- ⚠️ `OrderDetailPage`: يُعرَض `item_name_ar` فقط

**الإصلاح:** دالة موحَّدة في `utils/helpers.js`:
```javascript
export const displayItemName = (item) =>
  item?.item_name_ar?.trim() || item?.item_name_en?.trim() || item?.item_code || '—';
```

---

### 24. Base URL في Vite Proxy مُشفَّر (hardcoded)

**الملف:** `frontend/vite.config.js:9-12`

```javascript
proxy: { '/api': 'http://localhost:8000' }  // لن يعمل في production
```

يجب استخدام env variable: `VITE_API_BASE_URL`.

---

### 25. مجلد `dist/` محفوظ في الـ repo

ملف `.gitignore` غير موجود أصلاً في الـ frontend. `dist/` بحجم 812 KB في الـ git history.

---

### 26. validation للـ XLSX ضعيف

**الملف:** `app/routers/import_data.py:85-90`

الامتداد فقط يُفحَص، بدون فحص محتوى الملف. ملف xlsx خبيث (XXE، zip bomb) يسبب crash.

---

### 27. Partial Receipts غير مُصالحة

لو طُلب 100، صُرف 100، استُلم 60 (20 مفقودة، 20 تالفة):
- 20 وحدة **لا تُرجع للمستودع** رغم إنها مفقودة من الطلبية.
- 20 وحدة تالفة لا تدخل جدول "damage/write-off".

---

## 🟡 ثالثاً: المشاكل المتوسطة (Medium Priority)

### 28. Missing Indexes على الحقول الشائعة

جداول كبيرة (orders, audit_logs, stock_transactions) لا يوجد لها فهارس على:
- `branch_id`, `warehouse_id`, `status`, `created_at`
- `(order_id, item_id)` في `replenishment_order_lines`
- `(inventory_id, item_id)` في `daily_inventory_lines`

**الأثر:** مع نمو البيانات (مثلاً بعد سنة: 50,000 طلبية + 500,000 سطر) ستصبح الاستعلامات بطيئة جداً.

---

### 29. Enum String Columns بدون CHECK

```python
variance_status = Column(String(20))  # "ok" | "warning" | "critical" — لكن أي قيمة تُقبَل
line_status = Column(String(30))      # نفس المشكلة
```

**الإصلاح:** تحويل إلى `SAEnum` أو إضافة `CheckConstraint`.

---

### 30. Multi-Tenant Filtering غير مُطبَّق عالمياً

حسب `TENANT_ID_MIGRATION_PLAN.md`:
- ✅ Phase 1-3 (إضافة `tenant_id`) منجزة.
- ❌ Phase 4-6 (تطبيق الفلترة التلقائية) لم تنجز.

في `app/routers/alerts.py`:
```python
db.query(BranchStock).filter(Item.is_deleted == False)  # لا يفلتر tenant_id!
```

**لو تم تفعيل ميزة multi-tenant قبل إتمام Phase 6 → تسريب بيانات بين المستأجرين (tenants).**

---

### 31. Inventory Session Creation Race

**الملف:** `app/services/inventory_service.py:235-329`

طلبان متزامنان لإنشاء جرد لنفس الفرع واليوم قد ينجحان كلاهما (سباق بين SELECT و INSERT). **يجب إضافة UNIQUE constraint على (branch_id, inventory_date, tenant_id) مع معالجة `IntegrityError`.**

---

### 32. Soft Delete Filtering غير متسق

`is_deleted` عمود موجود على User, Item, Branch, Warehouse، لكن:
- ✅ `auth.py` يفلتره.
- ❌ بعض الـ reports لا يفلترونه → بيانات محذوفة تظهر في النتائج.

**الحل الأمثل:** SQLAlchemy event listener أو hybrid query base class يطبّق الفلتر تلقائياً.

---

### 33. Cascade Delete Policy غير موثَّق

بعض العلاقات cascade (DailyInventory.lines) وبعضها لا (Branch.orders). **لا يوجد توثيق واضح** لسبب الاختيار. يجب كتابة policy في README.

---

### 34. Silent Promise Rejection في الـ Frontend

**الملف:** `src/pages/delivery/DeliveryAnalyticsPages.jsx`

```javascript
.catch(() => {})  // ابتلاع الأخطاء!
```

المستخدم يرى شاشة فارغة دون معرفة السبب.

---

### 35. Confirm() الأصلي للمتصفح بدل Modal

استخدام `window.confirm()` في صفحات الجودة يكسر RTL + لا يتبع التصميم العام.

---

### 36. App.jsx ضخم (650 سطر)

عدة pages inline في ملف واحد (ManualOrderPage, BranchStockPage, WarehouseStockPage). يجب تقسيمها.

---

### 37. AppLayout.jsx ميت

`AppLayoutV2.jsx` هو المُستخدَم. `AppLayout.jsx` القديم يجب حذفه.

---

### 38. Async/await مفقود بعد الحفظ

**الملف:** `OrderDetailPage.jsx:53`

```javascript
warehouseReview(...);  // بلا await!
load();  // يُنفَّذ قبل اكتمال الحفظ
```

الواجهة تُحدَّث قبل أن يصل الحفظ للخادم. أحياناً يُعرض البيانات القديمة.

---

### 39. No Loading States أثناء الحفظ

الأزرار تبقى active بعد الضغط → المستخدم يضغط مرتين → طلبان متطابقان.

---

### 40. لا validation على قيم الكميات في الـ UI

حقل `counted_qty` يقبل سالب، رقم ضخم، نصوص. الفلترة على الـ backend فقط.

---

### 41. Responsive Design ناقص

`grid-cols-2 lg:grid-cols-4` بدون `sm:grid-cols-1` — على الموبايل الأعمدة تتكدس.

---

### 42. No Lazy Loading للـ Routes

كل الـ pages تُحمَّل مع الـ bundle الأولي. يجب `React.lazy()` للصفحات الثانوية.

---

### 43. نقص في تغطية الاختبارات للـ Race Conditions

الـ tests (4677 سطر) تغطي الـ happy path جيداً، لكن:
- لا يوجد اختبار concurrent للـ dispatch/receive.
- لا يوجد اختبار IDOR للـ ledger/variance.
- لا يوجد اختبار لـ partial receipt reconciliation.

---

### 44. Idempotency على بعض العمليات فقط

حسب `IDEMPOTENCY_PATTERN.md`: dispatch, receive, submit_to_warehouse فقط محمية. لكن `approve_order`, `warehouse_review`, `branch_review` غير محمية → موافقة مكررة ممكنة.

---

### 45. لا يوجد Logout Endpoint

التوكن صالح لحد 480 دقيقة. عند logout يتم مسح localStorage فقط، والتوكن يبقى صالحاً لو سُرِق.

---

## 🟢 رابعاً: المشاكل المنخفضة (Low Priority)

46. **توقيت هجري غير مدعوم** — تقارير السعودية أحياناً تتطلب تاريخ هجري.
47. **Arabic-Indic numerals** غير مستخدمة (٠١٢٣٤). الأرقام الغربية (0123) فقط.
48. **AppLayout.jsx ميت** وغير مستخدم.
49. **Float imported غير مستخدم** في `models/__init__.py`.
50. **`.catch()` ناقص** في معظم `useEffect` → الأخطاء تُبتلع.
51. **Dynamic imports داخل useEffect** بلا فائدة واضحة.
52. **Font fallback للـ CDN** غير موجود (لو Google Fonts عطلت).
53. **No `.gitignore`** في الـ frontend.
54. **`dist/` في الـ repo** يجب حذفه.
55. **No base path في Vite** → لا يمكن نشر التطبيق على `/inventory/`.

---

## 📊 خامساً: ملاحظات إيجابية (ما هو مصنوع بشكل جيد)

رغم المشاكل، النظام فيه أمور ممتازة يجب الحفاظ عليها:

- **Numeric(10,3) للكميات و Numeric(14,2) للأسعار** — لا يوجد رقم عائم (Float) في المال → احتراف.
- **Alembic migrations** منظمة بشكل خطي (بدون merge conflicts) — احتراف.
- **Idempotency Pattern** موثَّق ومُطبَّق على العمليات الحساسة.
- **Stock Ledger** موجود (ليس مجرد تعديل مباشر على المخزون).
- **Enums للـ status الأساسية** (OrderStatus, InventoryStatus, TransactionType).
- **RTL + Cairo font** مُهيّأ جيداً.
- **Soft delete** موجود على الكيانات المهمة.
- **4677 سطر tests** — تغطية معقولة للـ happy paths.

---

## 🛠️ سادساً: خطة الإصلاح المقترحة (Roadmap)

### المرحلة 1 — إيقاف النزيف (أسبوع واحد)

هدفها: منع **فساد البيانات** و **تسريبها**.

1. إصلاح IDOR في `ledger.py` و `variance report` ← **يوم واحد**.
2. إصلاح `transfer_warehouse_to_branch` للتحقق من المصدر ← **ساعات**.
3. إضافة `.with_for_update()` على كل mutations في stock ← **يوم**.
4. إصلاح idempotency في `approve_inventory` (إلزام client_request_id) ← **ساعات**.
5. إزالة Fallback لـ `SECRET_KEY` — إجباره من env ← **ساعات**.
6. إزالة demo credentials من LoginPage في production ← **دقائق**.
7. إعادة تشغيل الـ backend على جهازك ← **فوري**.

### المرحلة 2 — سلامة المنطق التجاري (أسبوعان)

8. فحص المخزون قبل موافقة المستودع (block over-approve).
9. رفع خطأ صريح بدل `max(0, qty - dispatch)`.
10. Reconciliation للـ partial receipts (rollback للمفقود).
11. إضافة CHECK constraints للكميات (`qty >= 0`).
12. إضافة FK على `source_id/destination_id` في `stock_transactions`.
13. Timezone handling (AST) في الـ backend.
14. Auto-replenishment scheduler (APScheduler cron).

### المرحلة 3 — تحسينات الجودة (شهر)

15. إضافة indexes على الحقول الشائعة.
16. إكمال Multi-Tenant Phase 4-6.
17. Error Boundaries في الـ Frontend.
18. نقل التوكن إلى httpOnly cookies.
19. توحيد `displayItemName()` helper.
20. إضافة tests للـ concurrency + IDOR.

### المرحلة 4 — ما هو متبقٍ من المهام الأصلية

21. تنفيذ `OrderType.inter_branch` كامل.
22. توليد users للفروع الـ 30 المتبقية.
23. Production build للـ frontend (يحل مشكلة cache Chrome).
24. صفحة مراجعة مدير المنطقة.

---

## 🔑 ملخص الملفات التي تحتاج تعديلاً

**Backend (بالأولوية):**

```
app/config.py                           # SECRET_KEY, DEBUG
app/routers/ledger.py                   # IDOR fix
app/services/ledger_service.py          # IDOR fix
app/services/stock_adjustment_service.py # warehouse access + locks
app/services/orders_service.py          # locks, stock check, reconciliation, negative guard
app/services/inventory_service.py       # idempotency, session race
app/services/replenishment_service.py   # scheduler integration
app/routers/auth.py                     # password validation unification
app/routers/import_data.py              # file size + content validation
alembic/versions/NEW_migration.py       # CHECK constraints, indexes, enum conversions
```

**Frontend (بالأولوية):**

```
src/pages/auth/LoginPage.jsx            # إزالة demo credentials في production
src/services/api.js                     # httpOnly cookies / إزالة localStorage
src/store/index.js                      # auth validation on load
src/App.jsx                             # Error Boundary + routes split
src/utils/helpers.js                    # displayItemName helper
vite.config.js                          # env-driven baseURL + base path
.gitignore (جديد)                       # إنشاؤه
```

---

## 📞 الأسئلة المطلوبة للمتابعة

قبل البدء بالإصلاح أحتاج إجابات على:

1. **هل البرنامج في production الآن أم مازال local؟** (يحدد الأولوية الزمنية)
2. **هل النظام سيُستخدم لأكثر من شركة (multi-tenant)؟** (يحدد إلزامية Phase 4-6)
3. **هل تريد من `wh.user.X` أن يرى فروعاً لم يُعيَّن عليها؟** (يحدد شدة RBAC)
4. **ما هي ساعة بداية اليوم تجارياً؟** (مهمة للـ timezone fix)
5. **ميزة `inter_branch` — هل هي عاجلة أم بعد إصلاح المشاكل الحرجة؟**

---

**نهاية التقرير** — تم إنتاجه بفحص آلي عميق لـ 100+ ملف. لا يُغني عن مراجعة بشرية نهائية قبل تطبيق الإصلاحات.
