# 🗺️ خطة تطوير نظام رائد للجرد — Roadmap

**الوضع الحالي (أبريل 2026):** النظام مستقر، الـ MVP شغّال، 32 فرع + 2 مستودع + 54 صنف. الـ test suite 92% passing بعد إصلاحات المراحل الست. الميزات الأساسية (جرد يومي، طلبيات، استلام، تحويلات، جودة، تدريب، تحليل توصيل) كلها شغّالة.

**السؤال الحقيقي ليس "ماذا نبني بعد ذلك"، بل "أين يضيع الوقت والمال فعلياً الآن؟"** الخطة أدناه مرتبة حسب أعلى أثر تجاري لسلسلة قهوة سعودية، مع الاعتبار أن الشبكة في الفروع قد تكون متقطعة وأن مدخلي البيانات هم موظفو فروع مش تقنيين.

---

## 🎯 المرحلة 7 — التحصين والأساس (4-6 أسابيع)

قبل أي ميزة جديدة، النظام محتاج بنية تحتية موثوقة. بدون كده، أي إضافة بتزيد الديون التقنية.

**أ. CI/CD وبيئة الاختبارات**
- GitHub Actions: `pytest` + `npm run build` + `npm run lint` على كل PR
- إصلاح الـ 15 اختبار المتبقي + `conftest.py` refactor
- Pre-commit hooks: black/ruff للـ Python، eslint للـ JS
- Branch protection على `main`: PR + review + checks passing

**ب. المراقبة والـ Observability**
- Sentry للأخطاء (backend + frontend) — أهم أداة هتوفرك عشرات الساعات debugging
- Structured logging (JSON) → أي log aggregator (Grafana Loki / CloudWatch)
- Uptime monitoring (UptimeRobot أو مشابه) على `/health`
- Dashboards: متوسط زمن الاستجابة، معدل الأخطاء، عدد الـ orders اليومية، عدد الجرد المتأخر

**ج. النسخ الاحتياطي والاسترجاع**
- `pg_dump` يومي تلقائي → S3/Backblaze
- اختبار استرجاع فعلي كل شهر (backup غير مختبر = لا backup)
- سكربت إعادة بناء DB من snapshot محدد

**د. الأمان**
- Rate limiting على `/auth/login` (5 محاولات/دقيقة/IP)
- 2FA اختياري لـ admin/super_admin (TOTP عبر Google Authenticator)
- Audit log للـ logins (IP + user agent + نجاح/فشل)
- HTTPS + HSTS إجباري في production (`nginx` / Cloudflare)

**هـ. التوثيق**
- `ADMIN_GUIDE.md` بالعربي لمدير النظام
- `BRANCH_USER_GUIDE.md` (مع صور) لمستخدمي الفروع
- OpenAPI auto-generated + نشره كـ docs site
- فيديو تدريبي قصير (5 دقائق) لكل دور

> **الخرج المتوقع:** ثقة في النظام، وقت استجابة أسرع للأخطاء، قدرة على التطوير بدون خوف من كسر الإنتاج.

---

## 📱 المرحلة 8 — تجربة الفرع: تقليل الاحتكاك اليومي (6-8 أسابيع)

موظف الفرع هو المستخدم الأكثر تعاملاً مع النظام يومياً. تحسين تجربته = تقليل أخطاء الجرد + توفير وقت = بيانات أنظف.

**أ. PWA + Offline-first**
- تحويل الـ React app لـ Progressive Web App
- Service worker يخزّن آخر بيانات جرد الفرع
- صفحة الجرد اليومي تشتغل offline، تـ sync تلقائياً لما ترجع الشبكة
- مؤشر حالة الشبكة في كل صفحة

**ب. قراءة الباركود**
- زر "مسح" في صفحة الجرد يفتح كاميرا الموبايل
- مكتبة: `html5-qrcode` أو `zxing-js`
- كل صنف يُخزَّن له `barcode` في جدول `items`
- يقلل أخطاء إدخال الكميات لصنف خطأ بنسبة تُقدّر بـ 70%+

**ج. الإشعارات**
نظام إشعارات موحد عبر:
- **In-app:** زرّ الجرس (`Bell icon` الموجود في الـ topbar حالياً، بس مش شغّال)
- **Email:** لمديري الفروع/المستودعات
- **SMS (اختياري):** للحالات الحرجة (طلبية مرفوضة، جرد متأخر أكثر من يوم)
- **Push (PWA):** للموبايل بعد تحويله لـ PWA

أهم الأحداث التي تستدعي إشعار:
- طلبية معتمدة → إشعار للمستودع
- طلبية مرفوضة → إشعار للفرع + مدير المنطقة
- جرد لم يُقدَّم قبل نهاية اليوم → إشعار لمدير الفرع والعمليات
- أصناف تحت الحد الأدنى → إشعار يومي لمدير الفرع
- زيارة جودة مكتملة → إشعار لمدير الفرع بالنقاط

**د. الجرد الذكي (ملء تلقائي)**
- حقل "الكمية المتوقعة" = `المخزون_السابق + الوارد - الصرف` (محسوب من الـ ledger)
- الموظف فقط يؤكد أو يعدّل الفارق → سرعة × 3
- تنبيه أوتوماتيكي عند فارق > 20% (احتمال خطأ أو هدر)

**هـ. تحسينات UX**
- Keyboard shortcuts في صفحة الجرد (Tab/Enter للتنقل السريع)
- "نسخ من الأمس" للأصناف اللي ما تغيرت
- فلاتر محفوظة (saved views) في قائمة الطلبيات
- Skeleton loaders بدل الـ spinners

> **الخرج المتوقع:** وقت الجرد اليومي يقل من 30 دقيقة إلى 10 دقائق لكل فرع. 32 فرع × 20 دقيقة توفير × 365 يوم = 3,890 ساعة سنوياً.

---

## 🧪 المرحلة 9 — الذكاء التشغيلي (8-12 أسبوع)

البيانات الآن بتتجمع منذ 4+ شهور. حان وقت استخدامها لاتخاذ قرارات.

**أ. Recipe / BOM (قائمة مكونات كل منتج مباع)**
الأكثر أهمية لسلسلة قهوة. كل "لاتيه متوسط" = 18g قهوة + 200ml حليب + 1 كوب + 1 غطاء.

- جدول جديد: `recipes` (item_sold_id → list of ingredient_items with qty)
- واجهة لمدير العمليات لتعريف الوصفات
- يربط استهلاك المواد الخام بالمبيعات

**ب. تكامل POS (نقطة البيع)**
- Webhook أو polling من نظام الـ POS الحالي (Foodics / LogoDesign / غيره)
- كل بيع يولّد تلقائياً `stock_transaction` من نوع `consumption` بناءً على الـ recipe
- نتيجة: مخزون الفرع يتحدّث لحظياً بدون تدخل يدوي
- الجرد اليومي يصبح مجرد *تأكيد* بدل إدخال كامل

هذه الميزة وحدها تحوّل النظام من "لوحة متابعة" إلى "نظام تشغيل".

**ج. توقع الطلب (Demand Forecasting)**
- بعد 6 أشهر بيانات: نموذج بسيط (ARIMA أو Prophet) لكل فرع × كل صنف
- توقّع الاستهلاك للأيام الـ 7 القادمة → تحسين الحد الأدنى والأقصى ديناميكياً
- أقوى أثناء مواسم الذروة (رمضان، الأعياد، افتتاح مدرسي)

**د. كشف الشذوذ**
- تنبيه تلقائي عند:
  - فرق جرد > 15% عن المتوقع لـ 3 أيام متتالية → احتمال هدر/سرقة
  - صنف لم يُستهلك في فرع منذ 30 يوم → احتمال صنف ميت يجب إزالته من الفرع
  - طلبية تم رفضها من المستودع بسبب "لا يوجد مخزون" > 5 مرات/شهر → مشكلة تخطيط

**هـ. تحليل التكاليف**
- حقل `cost_price` لكل صنف + تاريخ تكلفة (تتغير مع الموردين)
- تقرير: تكلفة البضاعة المباعة (COGS) لكل فرع/أسبوع
- تقرير الهدر (wastage) بالريال
- هامش الربح التقديري لكل منتج مباع

> **الخرج المتوقع:** قرارات تعتمد على بيانات بدلاً من حدس. كشف مبكر لتسريب المخزون. تحسين هامش الربح 2-5%.

---

## 🏢 المرحلة 10 — توسّع وامتداد (6-12 شهر)

بعد استقرار الأساس والذكاء، فكّر في النمو.

**أ. إدارة الموردين والمشتريات**
- جدول `suppliers` (ترتبط بـ `items` عبر `item_suppliers`)
- Purchase Orders للموردين (مختلفة عن الـ replenishment الداخلي)
- Lead time tracking
- Goods receipt vs invoice reconciliation
- تكامل مع محاسبة (QuickBooks / Zoho Books أو نظام محلي)

**ب. تتبع الصلاحية (Expiry)**
- حقل `expiry_date` على `branch_stock` (عدة صفوف لنفس الصنف بتواريخ مختلفة)
- FIFO تلقائي في الـ picking
- تقرير أصناف قاربت انتهاء الصلاحية
- مهم جداً للحليب، المعجنات، حبوب القهوة المحمّصة

**ج. الميزات الفرنشايز**
- Multi-company / Multi-brand (الـ tenant_id موجود بالفعل — فعّله)
- Portal لأصحاب الفرنشايز
- تقارير معزولة لكل فرنشايزي
- نموذج عمولات/رسوم

**د. Mobile App (Native)**
- Flutter/React Native
- خاص بمديري المناطق وزيارات الجودة
- التصوير + GPS tagging للزيارات
- توقيع المفتش والمدير إلكترونياً

**هـ. API عامة + Webhooks**
- REST API موثّق للتكامل مع أنظمة خارجية
- Webhooks للأحداث المهمة (order approved, stock low, etc.)
- API keys management
- Rate limiting per key

---

## 📊 مصفوفة الأولوية (تأثير × جهد)

| الميزة | التأثير | الجهد | الأولوية |
|---|---|---|---|
| CI/CD + Sentry | 🔴 حرج | 🟢 قليل | **فوراً** |
| Backups منتظمة | 🔴 حرج | 🟢 قليل | **فوراً** |
| الإشعارات | 🟡 عالي | 🟡 متوسط | المرحلة 8 |
| PWA + Offline | 🟡 عالي | 🔴 كبير | المرحلة 8 |
| Barcode scanning | 🟡 عالي | 🟢 قليل | المرحلة 8 |
| Recipe/BOM | 🔴 حرج | 🟡 متوسط | المرحلة 9 |
| POS integration | 🔴 حرج | 🔴 كبير | المرحلة 9 |
| Forecasting | 🟢 متوسط | 🔴 كبير | المرحلة 9 |
| Supplier mgmt | 🟡 عالي | 🔴 كبير | المرحلة 10 |
| Expiry tracking | 🟡 عالي | 🟡 متوسط | المرحلة 10 |
| Multi-tenant | 🟢 متوسط | 🔴 كبير | المرحلة 10 |

---

## 🤔 أسئلة استراتيجية قبل البدء

قبل تنفيذ أي مرحلة، جاوب على الأسئلة دي — إجاباتك بتعيد ترتيب الأولويات:

1. **ما هو نظام الـ POS الحالي؟** (Foodics / Odoo / مخصص؟) لو في API جاهز، المرحلة 9-ب تقفز لأولوية أعلى.

2. **هل الفروع عندها شبكة واي-فاي مستقرة؟** لو لا، PWA+Offline ينتقل من المرحلة 8 لأولوية فورية.

3. **ما أكبر سبب للفاقد الحالي؟** هدر؟ سرقة؟ صلاحية منتهية؟ طلبيات زيادة؟ الإجابة بتوجّه المرحلة 9.

4. **هل في خطة توسع (فروع جديدة) خلال 6 شهور؟** لو نعم، أولوية التحصين ترتفع (ما ينفعش تفتح 10 فروع جدد على نظام مش متراقب).

5. **ميزانية الفريق؟** مطور واحد full-time؟ اثنين؟ بالنسبة لفريق 2-3 مطورين، الجدول أعلاه واقعي. لفريق مطور واحد، تمدد الزمن × 2.

6. **هل هتبيع النظام لسلاسل أخرى؟** لو نعم، المرحلة 10-ج (Multi-tenant) تقفز لأعلى، وفي قرارات معمارية مبكرة لازم تتاخد.

---

## 📌 توصيتي الشخصية للـ 3 شهور القادمة

لو كنت مكانك، هبدأ بالترتيب التالي بدون انحرافات:

**الشهر 1:** المرحلة 7 كاملة (CI + Sentry + Backups + 2FA). لا ميزات جديدة.

**الشهر 2:** من المرحلة 8 → Barcode + Notifications + Smart Inventory prefill. اترك PWA للشهر 3.

**الشهر 3:** PWA + offline + استكشاف تكامل الـ POS (discovery فقط، مش تنفيذ).

بنهاية 3 شهور، النظام يبقى: موثوق (CI + monitoring)، سريع الاستخدام (barcode + smart fill)، مُخبِر (notifications)، ومتسامح مع الشبكة (offline). ده الأساس المتين لأي ميزة ذكية تبنيها بعدين.

أي ميزة ذكية (forecasting/POS integration) قبل ما تتبني الأساس = هدر موارد وعواقب متأخرة.
